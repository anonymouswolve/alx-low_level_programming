#include "main.h"

/**
* mul - multiples of two integers
* @x: The first number to multiply
* @y: The second number to multipl with the first number
* Return: The result of multiplication between x and y
*/

int mul(int x, int y)
{
	return(x * y);
}

#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry Point
 * Return: Alwaysw 0 (Success)
 */
int main(void)
{
char lup;
for (lup = 'z'; lup >= 'a'; lup--)
putchar(lup);
putchar('\n');
return (0);
}

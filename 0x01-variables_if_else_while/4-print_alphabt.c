#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main -Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
char lup, e, q;
e = 'e'
q = 'q'
for (lup = 'a'; lup <= 'z'; lup++)
{
if (lup != e && lup != q)
putchar(lup);
}
puchar('\n');
return (0);
}
